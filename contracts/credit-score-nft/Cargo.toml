[package]
name = "credit-score-nft"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "20.0.0"
stellai_lib = { path = "../../lib" }

[dev-dependencies]
soroban-sdk = { version = "20.0.0", features = ["testutils"] }
